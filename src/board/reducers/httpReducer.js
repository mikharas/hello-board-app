const initialState = {
  isLoading: false,
  error: null,
};

const httpReducer = (state = initialState, action) => {
  switch (action.type) {
    case 'SEND_REQUEST':
      const {  } = action

      break;

    default:

  }
};
